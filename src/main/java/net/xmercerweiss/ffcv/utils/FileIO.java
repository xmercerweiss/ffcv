package net.xmercerweiss.ffcv.utils;

/**
 * A utility class responsible for handling file input,
 * output, and modification. Used primarily by the
 * {@link net.xmercerweiss.ffcv.app.FileRegistrar} and
 * {@link net.xmercerweiss.ffcv.app.FileProcessor} classes.
 */
public class FileIO {

    /**
     * If possible, converts the file of the given path
     * to the given format.
     *
     * @param path a file path
     * @param format the new file format
     */
    public static void convertToFormat(String path, String format) {

    }
}
